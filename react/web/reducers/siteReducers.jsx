import {ADD_OPTION,EDIT_OPTION,DEL_OPTION} from '../constants/SiteTypes';
import DataSam from '../data';
const initialState={
		
	isPopup:false,
	
};
export default function siteReduce(state=initialState,action){

	switch(action.type){


		default:
		return state;
	}

} 